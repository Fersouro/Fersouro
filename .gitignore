.venv/
__pycache__/
*.pyc

# Nunca versione chaves de service account.
*.json.key
service-account*.json
gcp-key*.json

# Saidas dos scripts de inspecao. Carregam DDL de views e SQL de aplicacao —
# logica de negocio do datalake — e este repositorio e o perfil publico.
metadados-bq-*/
dependencias-*/

# Perfis das views: carregam minimo, maximo e soma de cada coluna, ou seja,
# valores reais do negocio. Nao sao "so metadados".
perfil-*.json
